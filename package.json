// {
//     "scripts": {
//         "build": "cd astro-site && yarn install && yarn run build",
//         "postinstall": "cd astro-site && yarn install"
//     }
// }


{
    "scripts": {
        "build": "cd astro-site && nvm install 16.12.0 && yarn install && yarn run build",
        "postinstall": "cd astro-site && yarn install"
    }
}
